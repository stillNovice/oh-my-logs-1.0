html
  head
    title= title
    script(src='/lib/jquery-3.1.1.js')
  body
    h1= msg
    #fileList
      ul
        each val in arr
          li 
            a(href='?dir=' + prev + '/' + val)= val
